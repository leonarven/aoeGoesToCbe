Global SW,SH 'screen size
Global TS 'tile size
Global MW, MH 'map size

SW = 640 'screen width
SH = 480 'screen height

TS = 16 'tile size

MW = 32 'map width
MH = 32 'map height

SCREEN SW, SH

Include "customfunctions.cb"
Include "types.cb"
'Include "functions.cb"

cbeShowConsole(True)


Dim map(MW, MH)

'initMap()

Repeat
	Line 0,0,Rand(255),Rand(255)
	
	
	
	Text 0,0,map(0,0)
	Text 0,20,map(1,1)
	Text 0,40,map(2,2)
	
	
	
'	drawWorld()

	SetWindow "FPS: " + FPS()
	DrawScreen
Forever
